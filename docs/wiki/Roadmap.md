# Roadmap

A fonte detalhada é o arquivo **`DINO_MASTER_PROMPT.md`** no repositório.

## No GitHub

- **Project:** [Dino — produto & engenharia](https://github.com/users/adventurelabsbrasil/projects/7) — issues e PRs.
- **Épico:** issue **#22** (ordem sugerida: schema estendido → Telegram → IA → Sheets → N8N → páginas web…).
- **Label `plano-dino`:** filtra entregas do master prompt.

## Macro-fases

1. **MVP** — feito (auth, dashboard, transações).
2. **Dados estendidos** — bills, orçamento, conciliação, `ai_memory`, `category_rules` (#7).
3. **Canais** — Telegram (#8), Sheets (#10), N8N (#11).
4. **IA** — Claude + regras + memória (#9).
5. **Produto web** — `/bills`, `/budget`, `/reconciliation`, `/reports`, `/settings` (#12–#16).
6. **Qualidade** — anti-duplicata, roles (#18), CI (#19), E2E (#20).
